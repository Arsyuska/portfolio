---

---
<section>
    <img src="/Mask%20group(1).svg" alt="face" />
    <h1>Hi, I am Arsen Yelashkin</h1>
    <h2>I do Code & <b class="chill">Chill</b></h2>
    <p>Full-Stack Developer with expertise in building scalable web and mobile applications,
        dedicated to crafting elegant.Passionate about creating clean, high-performance solutions from start to finish.
    </p>
    <a href="#contact">Contact Me</a>
</section>

<style>
        section {
            display: flex;
            flex-direction: column;
            align-items: center;
            margin-block-start: 100px;
            font-family: "Poppins", sans-serif;
        }
        img {
            border: 2px solid #1A1A1A;
            background-color: #1A1A1A;
            border-radius: 50%;
            height: 230px;
            width: 230px;
        }
        h1  {
            font-size: clamp(40px, 5vw, 64px);
            margin: 0;
            padding-top: 45px;
            text-align: center;
        }


        h2  {
            font-size: 26px;
            background: linear-gradient(135deg, #f5f5f5 0%, #e0e0e0 50%, #f5f5f5 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .chill {
            background: linear-gradient(135deg, #6b48ff 0%, #ff5e8c 50%, #ff8c66 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        p {
            font-size: clamp(16px, 2vw, 22px );

            max-width: 600px;
            text-align: center;
        }

        a {
            border: 1px solid #585858;
            background-color: black;
            color: white;
            padding: 18px 60px;
            border-radius: 25px;
            cursor: pointer;
            font-size: 15px;
            font-weight: bold;
            margin-block: 20px;
            text-decoration: none;
        }

@media screen  and (max-width: 768px) {
    section {
        flex-direction: column;
    }
}

</style>