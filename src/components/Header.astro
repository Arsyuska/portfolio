---

---

    <header>
        <h1><a href="#" class="logo-link">CodeByArsen</a></h1>
        <nav>
            <ul>
                <li><a href="#about">About me</a></li>
                <li><a href="#contact" class="btn">Contact me</a></li>
            </ul>
        </nav>
</header>

<style>
    header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        font-family: "Poppins", sans-serif;
    }

    .logo-link {
        text-decoration: none;
        color: inherit;
        cursor: pointer;
    }

    .logo-link:hover {
        opacity: 0.8;
        transition: opacity 0.3s ease;
    }
    ul {
        list-style-type: none;
        display: flex;
        align-items: center;
        padding: 0;
        gap: 10px
    }

    li {
        border: 1px solid white;
        padding: 8px 10px;
        border-radius: 15px;
        font-size: 14px;
        transition: all 0.2s ease-out;
    }
    li a{
        color: white;
        text-decoration: none;
    }
    li:last-child {
        background-color: white;
    }
    li:last-child a{
        color: black;
    }
    nav li:hover {
        transform: scale(1.10) translateY(-6px);
        box-shadow:
                0 15px 30px rgba(156, 131, 255, 0.3),
                0 10px 20px rgba(255, 144, 81, 0.25);
        border-color: #9C83FF;
    }

    /* Спеціально для "Contact me" — білий фон */
    nav li:last-child {
        background-color: white;
    }

    nav li:last-child a {
        color: black;
    }

    /* При ховері на "Contact me" — трохи підсилюємо текст */
    nav li:last-child:hover a {
        color: #000;
        font-weight: 600;
    }

    /* Опціонально: легке підсвічування для "About me" при ховері */
    nav li:first-child:hover {
        background-color: rgba(255, 255, 255, 0.1);
    }

@media screen and (max-width: 768px) {
    header {
        flex-direction: column;
    }
    h1 {
        margin-block-end: 0;
    }

}
</style>